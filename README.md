# Python-Image-Duplicate-Finder
This is a python program that uses detects and deletes duplicate images at the user prompt
Works by calculating the hash of the image and not by size or name of the image
